% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duckdb_to_parquet.R
\name{duckdb_to_parquet}
\alias{duckdb_to_parquet}
\title{Export a DuckDB-backed lazy table to Parquet via COPY}
\usage{
duckdb_to_parquet(data, name, schema, data_dir = NULL, overwrite = TRUE)
}
\arguments{
\item{data}{A DuckDB-backed dbplyr table / lazy query (e.g., a \code{tbl_duckdb_connection}).}

\item{name}{File stem (used to create \verb{\{name\}.parquet}).}

\item{schema}{Subdirectory within \code{data_dir} to write to (can be \code{""}).}

\item{data_dir}{Base directory of your data repository. If \code{NULL}, uses \code{Sys.getenv("DATA_DIR")}.}

\item{overwrite}{Logical; overwrite existing Parquet file?}
}
\value{
A dbplyr table (lazy) reading the written Parquet file.
}
\description{
Writes the result of a DuckDB query (or table) to a Parquet file using
DuckDB's \verb{COPY (SELECT ...) TO ... (FORMAT PARQUET)} and returns a dbplyr
table that reads the written Parquet file.
}
